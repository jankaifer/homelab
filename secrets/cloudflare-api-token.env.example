# Cloudflare API Token for Caddy DNS challenge
#
# Create a token at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit for your domain
#
# Copy this file to cloudflare-api-token.env and fill in your token:
#   cp secrets/cloudflare-api-token.env.example secrets/cloudflare-api-token.env
#
# The .env file is gitignored and won't be committed.

CLOUDFLARE_API_TOKEN=your-api-token-here
# Used by NixOS security.acme (lego Cloudflare provider)
CLOUDFLARE_DNS_API_TOKEN=your-api-token-here
