# Nix build outputs
result
result-*

# Editor backups
*~
*.swp
.*.swp

# macOS
.DS_Store

# Secrets (encrypted files are OK, but don't commit decrypted)
secrets/*.txt
secrets/*.key
secrets/*.env
!secrets/*.age

# Local keys (SSH keys for agenix)
keys/

# VM disk images
*.qcow2

# ISO images
*.iso
